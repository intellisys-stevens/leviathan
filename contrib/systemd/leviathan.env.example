# Optional collector cadences. Dashboard snapshots may remain faster.
LEVIATHAN_PROFILE_INTERVAL=2s
LEVIATHAN_PROCESS_INTERVAL=2s

# Enable only when the optional Kubernetes attribution bridge is installed.
# LEVIATHAN_ATTRIBUTION_SOCKET=/run/leviathan/attribution.sock
