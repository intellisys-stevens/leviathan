# Credential-free HTTPS origin of the central MIGLens Hub reverse proxy.
MIGLENS_HUB_URL=https://miglens-hub.example.test

# Set a random creator-scoped token through cloud-init or a secret manager.
# Keep the installed environment file root-owned and mode 0600.
MIGLENS_UPLINK_TOKEN=
